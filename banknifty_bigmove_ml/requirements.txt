# Local / CPU-side pipeline deps (Phase 1 — labeling, splits, DVC).
# Training-image deps (torch, optuna, mlflow client) live in infra/Dockerfile.train
# since they need a CUDA base image and don't belong on the laptop.
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
dvc>=3.50
mlflow>=2.14
pytest>=7.4
