# Installed on top of the pytorch/pytorch CUDA base image (infra/Dockerfile.train).
# torch itself comes from the base image, not listed here.
# mlflow MUST match the server's pinned version exactly (infra/Dockerfile.mlflow) —
# client/server version drift (e.g. a 3.x client against a 2.16 server) breaks
# newer REST endpoints like /api/2.0/mlflow/logged-models with a silent-looking
# 404 that only surfaces deep in mlflow.pytorch.log_model()'s traceback.
mlflow==2.16.0
optuna>=3.6
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
scikit-learn>=1.5
dvc>=3.50
